import pandas as pd
from sklearn.preprocessing import MinMaxScaler, StandardScaler
df = pd.DataFrame({'Marks': [20, 40, 60, 80, 100]})

df['MinMax'] = MinMaxScaler().fit_transform(df[['Marks']])
df['Zscore'] = StandardScaler().fit_transform(df[['Marks']])

print(df)
